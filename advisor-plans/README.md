# Implementation Plans

Generated by the improve skill on 2026-07-01 for branch `ec/region-reuse-support` at commit `eeb1f79`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

The repository already has a `plans/` directory for product and Windows roadmap documents, so these advisor-generated implementation plans live under `advisor-plans/`.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Preselect remembered regions on every display | P1 | S | - | DONE |
| 002 | Keep remembered region selected until the user drags | P1 | S | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- 002 depends on 001 because both touch `mac/TinyClips/Capture/RegionSelector.swift`; doing the API/display-scope change first reduces conflict risk.

## Findings Considered And Rejected

- Security issue from remembered regions: rejected because the store is in-memory only and does not persist capture coordinates.
- Windows parity issue: rejected for this branch because commit `eeb1f79` intentionally made the branch mac-only.
- Performance issue from dictionary lookups and overlay drawing: rejected because the feature adds constant-time in-memory lookups and no hot-path capture processing.
